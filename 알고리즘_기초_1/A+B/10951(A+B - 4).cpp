#include <bits/stdc++.h>

int main(){
    using namespace std;
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    auto a = 0, b = 0;

    // 테스트 개수가 주어지지 않는 경우 
    while(cin >> a >> b)
        cout << a + b << endl;

    return 0;
}